# Senda - Adventure

Notes for deployment:
- Use [PM2](https://pm2.keymetrics.io/) for production process managing => Maybe we should add the .env secrets via terminal in order to avoid uploading the .env file
- Use [Nginx](https://nginx.org/) for server proxy

